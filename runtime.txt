python-3.7.4

# r_one = Room(title="Outside Cave Entrance",
#                description="North of you, the cave mount beckons")

# r_two = Room(title="Foyer", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_three = Room(title="Grand Overlook", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_four = Room(title="Narrow Passage", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_five = Room(title="Treasure Chamber", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ##############################

# r_six = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_seven = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_eight = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_nine = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_ten = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")
# ######################################

# r_eleven = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_twelve = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_thirteen = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_fourteen = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_fiteen = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ############################################

# r_sixteen = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_seventeen = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_eightteen= Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_nineteen = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_twenty = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ########################################


# r_twentyone = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_twentytwo = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_twentythree = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_twentyfour = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_twentyfive = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################

# r_twentysix = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_twentyseven = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_twentyeight = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_twentynine = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_thirty = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# #############################

# r_thirtyone = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_thirtytwo = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_thirtythree = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_thirtyfour = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_thirtyfive = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# #############################################


# r_thirtysix = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_thirtyseven = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_thirtyeight = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_thirtynine = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_fourty = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################


# r_fourtyone = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_fourtytwo = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_fourtythree = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_fourtyfour = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_fourtyfive = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################


# r_fourtysix = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_fourtyseven = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_fourtyeight = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_fourtynine = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_fifty = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################

# r_fiftyone = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_fiftytwo = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_fiftythree = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_fiftyfour = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_fiftyfive = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################

# r_fiftysix = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_fiftyseven = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_fiftyeight = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_fiftynine = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_sixty = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################

# r_sixtyone = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_sixtytwo = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_sixtythree = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_sixtyfour = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_sixtyfive = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################

# r_sixtysix = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_sixtyseven = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_sixtyeight = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_sixtynine = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_seventy = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################


# r_seventyone = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_seventytwo = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_seventythree = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_seventyfour = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_seventyfive = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################


# r_seventysix = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_seventyseven = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_seventyeight = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_seventynine = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_eighty = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################


# r_eightyone = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_eightytwo = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_eightythree = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_eightyfour = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_eightyfive = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################

# r_eightysix = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_eightyseven = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_eightyeight = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_eightynine = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_ninety = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################


# r_ninetyone = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_ninetytwo = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_ninetythree = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_ninetyfour = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_ninetyfive = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################


# r_ninetysix = Room(title="Harry",
#                description="North of you, the cave mount beckons")

# r_ninetyseven = Room(title="peter", description="""Dim light filters in from the south. Dusty
# passages run north and east.""")

# r_ninetyeight = Room(title="ground", description="""A steep cliff appears before you, falling
# into the darkness. Ahead to the north, a light flickers in
# the distance, but there is no way across the chasm.""")

# r_ninetynine = Room(title="herbert", description="""The narrow passage bends here from west
# to north. The smell of gold permeates the air.""")

# r_onehundred = Room(title="love", description="""You've found the long-lost treasure
# chamber! Sadly, it has already been completely emptied by
# earlier adventurers. The only exit is to the south.""")

# ###############################################################

# rows = 10
# cols = 10

# grid = [ [r_one,
# r_two,
# r_three,
# r_four,
# r_five,
# r_six,
# r_seven,
# r_eight,
# r_nine,
# r_ten],
# [
# r_eleven,
# r_twelve,
# r_thirteen,
# r_fourteen,
# r_fiteen,
# r_sixteen,
# r_seventeen,
# r_eightteen,
# r_nineteen,
# r_twenty],
# [
# r_twentyone,
# r_twentytwo,
# r_twentythree,
# r_twentyfour,
# r_twentyfive,
# r_twentysix,
# r_twentyseven,
# r_twentyeight,
# r_twentynine,
# r_thirty],
# [
# r_thirtyone,
# r_thirtytwo,
# r_thirtythree,
# r_thirtyfour,
# r_thirtyfive,
# r_thirtysix,
# r_thirtyseven,
# r_thirtyeight,
# r_thirtynine,
# r_fourty],
# [
# r_fourtyone,
# r_fourtytwo,
# r_fourtythree,
# r_fourtyfour,
# r_fourtyfive,
# r_fourtysix,
# r_fourtyseven,
# r_fourtyeight,
# r_fourtynine,
# r_fifty],
# [
# r_fiftyone,
# r_fiftytwo,
# r_fiftythree,
# r_fiftyfour,
# r_fiftyfive,
# r_fiftysix,
# r_fiftyseven,
# r_fiftyeight,
# r_fiftynine,
# r_sixty],
# [
# r_sixtyone,
# r_sixtytwo,
# r_sixtythree,
# r_sixtyfour,
# r_sixtyfive,
# r_sixtysix,
# r_sixtyseven,
# r_sixtyeight,
# r_sixtynine,
# r_seventy],
# [
# r_seventyone,
# r_seventytwo,
# r_seventythree,
# r_seventyfour,
# r_seventyfive,
# r_seventysix,
# r_seventyseven,
# r_seventyeight,
# r_seventynine,
# r_eighty],
# [
# r_eightyone,
# r_eightytwo,
# r_eightythree,
# r_eightyfour,
# r_eightyfive,
# r_eightysix,
# r_eightyseven,
# r_eightyeight,
# r_eightynine,
# r_ninety],
# [
# r_ninetyone,
# r_ninetytwo,
# r_ninetythree,
# r_ninetyfour,
# r_ninetyfive,
# r_ninetysix,
# r_ninetyseven,
# r_ninetyeight,
# r_ninetynine,
# r_onehundred]]

# for i in range(rows):
#   for j in range(cols):
#     grid[i][j].save()






















# STREET

# streetRooms = {title: Room(title=title, description=title) for title in range(100, 200)}

# streetGrid = []
# tempStreetArray = []
# keepTrackOfStreetIndex = 109
# # rows=10
# # cols=10

# for key, value in streetRooms.items():
#   if keepTrackOfStreetIndex == key:
#     keepTrackOfStreetIndex += rows
#     tempStreetArray.append(value)
#     streetGrid.append(tempStreetArray)
#     tempStreetArray = []
#   else:
#     tempStreetArray.append(value)
 
# for i in range(rows):
#   for j in range(cols):
#     streetGrid[i][j].save()

# for i in range(rows):
#   for j in range(cols):
#     if i < rows - 1:
#       streetGrid[i][j].connectRooms(streetGrid[i + 1][j], "s")
#     if i > 0:
#       streetGrid[i][j].connectRooms(streetGrid[i - 1][j], "n")  
#     if j < cols - 1:
#       streetGrid[i][j].connectRooms(forestGrid[i][j + 1], "e")  
#     if j > 0:
#       streetGrid[i][j].connectRooms(streetGrid[i][j - 1], "w")